#include<iostream>
using namespace std;
void reverse(string str)
{
for(int i=str.length()-1;i>=0;i--)
cout<<str[i];
}
int main(){
string name;
cout<<"enter a string :";
cin>>name;
reverse(name);
return 0;
}
